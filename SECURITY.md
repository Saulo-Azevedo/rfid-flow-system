# Security Policy

## Reportando vulnerabilidades
- Não compartilhe senhas/SECRET_KEY em issues
- Para reportes, use um canal privado definido com o cliente

## Boas práticas
- `DEBUG` deve ser `False` em produção
- `SECRET_KEY` e credenciais devem ficar **apenas** em variáveis de ambiente
